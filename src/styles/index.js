import Colors from '../styles/Colors';
import {Dimensions} from 'react-native';
import Typography from './Typography';
const {height, width} = Dimensions.get('window');

export {Colors, height, width, Typography};
